# FastAPI Backend

The FastAPI backend serves the machine learning models and provides endpoints for the frontend and external services.
- **Port**: 4308
- Interfaces with the PostgreSQL database to fetch real-time alerts.
- Connects to the MLflow model registry to load the latest active intrusion detection model.
